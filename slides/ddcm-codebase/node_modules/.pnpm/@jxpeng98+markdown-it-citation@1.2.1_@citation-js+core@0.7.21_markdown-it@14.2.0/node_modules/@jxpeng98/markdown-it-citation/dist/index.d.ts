import MarkdownIt from 'markdown-it';
interface CitationOptions {
    bibFile?: string;
    style?: string;
    showNum?: boolean;
    bibTitle?: string;
    autoGenerate?: boolean;
}
export default function citationPlugin(md: MarkdownIt, options?: CitationOptions): void;
export {};
